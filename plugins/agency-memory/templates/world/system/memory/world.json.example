{
  "_comment": "World config = the instance layer. The plugin ships English defaults (world.default.json); this file OVERRIDES any subset of them. You only need the keys you actually change. To activate: copy to world.json (same folder) and edit. This example shows a Hungarian instance.",

  "_comment_sections": "If you localize section names here, your learnings.md / system learnings.md section headers MUST match these strings exactly (the engine splits on them).",

  "next_briefing_heading": "Következő session briefing",

  "sections": [
    "Kampány learningek",
    "Mi nem működik",
    "Ügyfél preferenciák",
    "Döntési log",
    "Következő session briefing",
    "Próbált és elvetett"
  ],

  "system_sections": [
    "Architektúra döntések",
    "Workflow tanulságok",
    "Tooling minták",
    "Fejlesztői döntések",
    "Következő session briefing",
    "Próbált és elvetett"
  ],

  "protected_sections": [
    "Kampány learningek",
    "Mi nem működik",
    "Ügyfél preferenciák",
    "Döntési log",
    "Próbált és elvetett"
  ],

  "protected_system_sections": [
    "Architektúra döntések",
    "Workflow tanulságok",
    "Tooling minták",
    "Fejlesztői döntések",
    "Próbált és elvetett"
  ],

  "evergreen_sections": [
    "Ügyfél preferenciák",
    "Próbált és elvetett"
  ],

  "curated_prefix": "kampanyok-",

  "_comment_i18n": "CRITICAL for a non-English world: without these four keys the engine falls back to ENGLISH defaults - the consolidation prompt will then instruct the model to write (i.e. TRANSLATE your data to) English, placeholder sections are mis-detected and the footer leaks into section bodies. If your world is not English, set ALL FOUR. (A language-drift guard skips the write if this happens, but fix the config, do not rely on the guard.)",
  "output_language": "Hungarian",
  "placeholder_markers": ["_Még nincs", "_Nincs aktív", "_Induló"],
  "placeholder": "_Még nincs adat._",
  "footer_prefix": "_Utolsó frissítés",

  "_comment_briefing_retention": "Optional. briefing_keep_checkpoints > 0 = the weekly system consolidation keeps only the newest N checkpoint blocks in the next-briefing section and sweeps the rest to system/memory/archive/briefing-archive.md (nothing is deleted). 0 (default) = off, the section only grows. Blocks are recognized by briefing_block_regex (default: lines starting with **...CHECKPOINT).",
  "briefing_keep_checkpoints": 1,

  "_comment_memory_guard": "Optional. project_memory_path = where the agent's global auto-memory lives (null = derive from world root, matching Claude Code's ~/.claude/projects/<slug>/memory scheme). guards = file-path patterns -> memory files to surface before a Write/Edit. Empty guards = the PreToolUse hook is a no-op. The no-mixing check (warn when a write under clients/<X>/ mentions another client) is automatic and needs no config.",
  "memory_guard": {
    "project_memory_path": null,
    "guards": [
      {
        "patterns": ["clients/", "riports/", "audits/"],
        "memories": ["feedback_no_write_without_approval.md"],
        "label": "Client Output"
      }
    ]
  }
}

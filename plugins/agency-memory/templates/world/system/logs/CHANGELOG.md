# Changelog

_System/dev changes (scripts, hooks, config, infra). Append newest at the top with a date
and a short entry. The agent appends here at the end of a system/dev session; the weekly
consolidation reads it to refine the system learnings (it never writes here itself)._

---
